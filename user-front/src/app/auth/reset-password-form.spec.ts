import { ResetPasswordForm } from './reset-password-form';

describe('ResetPasswordForm', () => {
  it('should create an instance', () => {
    expect(new ResetPasswordForm()).toBeTruthy();
  });
});
