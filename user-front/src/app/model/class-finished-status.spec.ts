import { ClassFinishedStatus } from './class-finished-status';

describe('ClassFinishedStatus', () => {
  it('should create an instance', () => {
    expect(new ClassFinishedStatus()).toBeTruthy();
  });
});
